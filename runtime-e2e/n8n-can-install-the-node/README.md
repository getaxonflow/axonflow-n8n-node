# n8n-can-install-the-node

Verifies that the built `@axonflow/n8n-nodes-axonflow` package installs into n8n
and that n8n's node type registry includes the `axonFlow` node type.

This is the most basic smoke test: if this fails, no other tests can run.
